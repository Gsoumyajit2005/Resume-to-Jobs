{
  "name": "resume-matcher-backend",
  "version": "1.0.0",
  "description": "Backend API for Resume-to-Jobs Matching Platform",
  "scripts": {
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "dev": "uvicorn main:app --host 0.0.0.0 --port 8000 --reload",
    "docs": "uvicorn main:app --host 0.0.0.0 --port 8000 --reload --docs"
  },
  "dependencies": {
    "fastapi[standard]": "^0.115.0",
    "pydantic": "^2.9.0",
    "langchain": "^0.3.0",
    "langchain-openai": "^0.2.0",
    "httpx": "^0.27.0",
    "beautifulsoup4": "^4.12.0",
    "PyPDF2": "^3.0.0",
    "python-docx": "^1.1.0"
  }
}
